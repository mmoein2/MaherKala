
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Master.cshtml";
}

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">سفارشات</h3>

            </div>
            <!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <br>
                @*@include('errors')*@
                <form action="/order" method="get">
                    <h4>جستجو</h4>
                    <div style="margin: 15px" class="row">
                        <div class="col-md-4">
                            <label>
                                نام
                            </label>
                            <input class="form-control" type="text" value="{{app('request')->buyer}}" name="buyer">
                        </div>
                        <div class="col-md-4">
                            <label>
                                وضعیت
                            </label>
                            <select name="isAdminShow" class="form-control select2" style="width: 100%;height: 100%">
                                @*<option value="0" @if ((app('request')->input('isAdminShow') == null)) selected @endif> همه</option>
                                <option value="1" @if ((app('request')->input('isAdminShow') == 1)) selected @endif> مشاهده شده</option>
                                <option value="2" @if ((app('request')->input('isAdminShow') == 2)) selected @endif> مشاهده نشده</option>*@
                            </select>
                        </div>
                        <div class="col-md-4">
                            <button style="margin-top: 25px" class="btn btn-primary" type="submit">جستجو</button>
                            <a style="margin-top: 25px" class="btn btn-danger" href="/order">حذف فیلتر</a>
                        </div>
                </form>
            </div>
            <hr>
            <table class="table table-hover">
                <tr>
                    <th>خریدار</th>
                    <th>تاریخ</th>
                    <th>تلفن</th>
                    <th>وضعیت</th>
                    <th>عملیات</th>
                </tr>
                @*@foreach ($orders as $order)
                <tr>
                    <td>{{$order->buyer}}</td>
                    <td>{{$order->date()}}</td>
                    <td>{{$order->phone}}</td>
                    <td style="background-color: @if($order->isAdminShow==1) lightgreen @else orange @endif">@if($order->isAdminShow==1)مشاهده شده@elseمشاهده نشده@endif</td>
                    <td>
                        <a href="/order/{{$order->id}}" class="btn btn-success">مشاهده</a>
                        <a href="/order/remove/{{$order->id}}" class="btn btn-warning">حذف</a>
                    </td>
                </tr>
                @endforeach*@
            </table>
            {{$orders->appends($_GET)->links()}}
        </div>
        <!-- /.box-body -->
    </div>
    <!-- /.box -->
</div>
    </div>
